import React from 'react';

const Inventories = () => {
    return (
        <div>
            <h2 className='text-center fw-light'>Inventory Products</h2>
            
        </div>
    );
};

export default Inventories;